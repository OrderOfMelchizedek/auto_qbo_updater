# Flask Configuration
# Generate with: python -c 'import secrets; print(secrets.token_hex(32))'
FLASK_SECRET_KEY=your-secret-key-here-must-be-at-least-32-chars

# QuickBooks OAuth Configuration
QBO_CLIENT_ID=your-qbo-client-id-from-intuit-developer
QBO_CLIENT_SECRET=your-qbo-client-secret-from-intuit-developer
QBO_REDIRECT_URI=http://localhost:5000/qbo/callback
QBO_ENVIRONMENT=sandbox

# Google Gemini API Configuration
GEMINI_API_KEY=your-gemini-api-key-from-google-ai-studio
GEMINI_MODEL=gemini-2.5-flash-preview-04-17

# Optional: For production deployment
# REDIS_URL=redis://localhost:6379/0  # For local Redis
# REDIS_URL=redis://username:password@hostname:port/0  # For remote Redis
# For Heroku Redis: automatically set when you add Redis addon
# 
# Without REDIS_URL, sessions are stored in filesystem (development only)
# Note: Filesystem sessions don't work well with multiple workers/dynos
# 
# SENTRY_DSN=your-sentry-dsn-for-error-tracking

# Date validation configuration (optional)
# Donations older than this many days will show a warning (default: 365)
# DATE_WARNING_DAYS=365
# Donations with future dates more than this many days will be rejected (default: 7)
# FUTURE_DATE_LIMIT_DAYS=7

# Rate limiting configuration (optional)
# Maximum Gemini API calls per minute (default: 60)
# GEMINI_RATE_LIMIT_PER_MINUTE=60
# Maximum Gemini API calls per hour (default: 1500)
# GEMINI_RATE_LIMIT_PER_HOUR=1500